# Google Gemma OSS 27B (instruction-tuned) via vLLM — single-H200 target.
#
# License: Gemma Terms of Use (Google). Custom license — permits
# commercial use subject to the Prohibited Use Policy. Redistribution
# requires the license copy + attribution. NOT Apache 2.0; see
# docs/gemma_provisioning.md for the security-review implications.
#
# Model ID note (VERIFY BEFORE PULLING):
#   `google/gemma-3-27b-it` is the last public release my author is
#   confident in (Gemma 3, March 2025). If Gemma 4 has since dropped,
#   update the model_id below to the current variant — the plumbing
#   is identical. Check:
#     https://huggingface.co/google  (Gemma release cards)
#     https://ai.google.dev/gemma    (Google official landing)
#
# Spin up the server with:
#     scripts/setup_vllm_server.sh /data/llm_weights/gemma-3-27b-it 1 8000
kind: vllm
model_id: google/gemma-3-27b-it
base_url: http://localhost:8000/v1
api_key: EMPTY
timeout_s: 120.0
