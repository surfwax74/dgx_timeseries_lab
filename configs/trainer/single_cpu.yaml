_target_key: lightning      # key in TRAINER_REGISTRY

max_epochs: 1
batch_size: 64
window_length: 256
window_stride: 128
learning_rate: 0.001
seed: 0
device: cpu
precision: 32-true
strategy: auto
num_workers: 0
checkpoint_dir: checkpoints
